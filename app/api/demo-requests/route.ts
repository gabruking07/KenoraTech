import { NextResponse, type NextRequest } from "next/server";
import { isAuthorizedAdmin } from "@/lib/admin-auth";
import { getMongoDb } from "@/lib/mongodb";

export const runtime = "nodejs";
const clean = (value: unknown, limit = 1000) => typeof value === "string" ? value.trim().slice(0, limit) : "";
export async function POST(request: NextRequest) { try { const body = await request.json(); const projectId = clean(body.projectId, 64), projectTitle = clean(body.projectTitle, 160), name = clean(body.name, 160), email = clean(body.email, 320).toLowerCase(), reason = clean(body.reason, 1600); if (!projectId || !projectTitle || !name || !/^\S+@\S+\.\S+$/.test(email) || !reason) return NextResponse.json({ error: "Name, email, project, and reason are required." }, { status: 400 }); const db = await getMongoDb(); const recent = await db.collection("demoRequests").countDocuments({ email, projectId, createdAt: { $gte: new Date(Date.now() - 60 * 60 * 1000) } }); if (recent >= 3) return NextResponse.json({ error: "Please wait before sending another request for this demo." }, { status: 429 }); await db.collection("demoRequests").insertOne({ projectId, projectTitle, name, email, company: clean(body.company, 200), phone: clean(body.phone, 80), reason, message: clean(body.message, 3000), status: "PENDING", createdAt: new Date() }); return NextResponse.json({ ok: true }, { status: 201 }); } catch { return NextResponse.json({ error: "Unable to submit demo request." }, { status: 500 }); } }
export async function GET(request: NextRequest) { if (!isAuthorizedAdmin(request)) return NextResponse.json({ error: "Unauthorized." }, { status: 401 }); try { const db = await getMongoDb(); const requests = await db.collection("demoRequests").find({}).sort({ createdAt: -1 }).toArray(); return NextResponse.json({ requests: requests.map(({ _id, ...item }) => ({ id: _id.toString(), ...item })) }); } catch { return NextResponse.json({ error: "Unable to load demo requests." }, { status: 500 }); } }
